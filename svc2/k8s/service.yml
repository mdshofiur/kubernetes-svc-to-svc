apiVersion: v1
kind: Service
metadata:
  name: api.poridhi.io
  namespace: devops
  labels:
    app: backend-svc
spec:
  selector:
    app: backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
