apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-1-deployment
  labels:
    app: frontend-one
spec:
  replicas: 3
  selector:
    matchLabels:
      app: frontend-one
  template:
    metadata:
      labels:
        app: frontend-one
    spec:
      containers:
        - name: frontend
          image: shafikur/backend-one:3.0
          ports:
            - containerPort: 80
          env:
            - name: PORT
              value: "80"
            - name: URL
              value: http://backend-sv2-svc.devops.svc.cluster.local